Some citations in your final answer are invalid and must be corrected:
{{ problems }}

Use Read to confirm the real line count of each file, then return a corrected `<final_answer>`. Only
include citations whose paths exist and whose line ranges fall within the file.